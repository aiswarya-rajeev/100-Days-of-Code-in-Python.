print("Welcome to the tip calculator!")

bill = float(input("What was the total bill? $"))
tip = int(input("How much tip would you like to give? 10, 12, or 15? "))
num_of_people = int(input("How many people to split the bill? "))

total_bill = ((tip/100)*bill)+bill
bill_per_person = total_bill / num_of_people

#final_amount = round(bill_per_person, 2)
final_amount="{:.2f}".format(bill_per_person)
print(f"Each person should pay: ${final_amount}")

OR
#Step by step code:

print("Welcome to the tip calculator!")

bill = float(input("What was the total bill? $"))
tip = int(input("How much tip would you like to give? 10, 12, or 15? "))
people = int(input("How many people to split the bill?"))

tip_as_percent = tip / 100
total_tip_amount = bill * tip_as_percent
total_bill = bill + total_tip_amount
bill_per_person = total_bill / people

final_amount = round(bill_per_person, 2)
print(f"Each person should pay: ${final_amount}")

#READ
#If we provide the inputs as 150, 12 and 5, we get the output as 33.6. Since we need 2 decimal points, we should use:
#final_amount="{:.2f}".format(bill_per_person)
#So we will get the output as 33.60 with two decimal  points.
#Otherwise, we can use:
#final_amount = round(bill_per_person, 2)
0 comments on commit c88d1ca
